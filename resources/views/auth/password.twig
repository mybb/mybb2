{% extends "layouts.base" %}
{% block title %}
    {{ trans('passwords.resetpw') }}
{% endblock %}
{% block contents %}
    {% if session_has('status') %}
        <div class="alert alert-success">
            {{ session_get('status') }}
        </div>
    {% endif %}

    {% if errors.any %}
        <div class="alert alert-danger">
            <strong>Whoops!</strong> There were some problems with your input.<br><br>
            <ul>
                {% for error in errors.all %}
                    <li>{{ error }}</li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}

    <form method="POST" action="{{ url('password/email') }}">
    <article class="centered-full-width">
        <input type="hidden" name="_token" value="{{ csrf_token() }}">

        <header>
            <h1>{{ trans('passwords.resetpw') }}</h1>
        </header>

        <section class="form">
            <div class="form-section clearfix">
                <div class="form-row">
                    <h3><label for="email">{{ trans('member.email') }}</label></h3>
                    <input type="text" name="email" id="email" value="{{ input_old('email') }}" />
                </div>
            </div>
        </section>

        <div class="form-submit">
            <button type="submit" class="button"><i class="fa fa-sign-in"></i><span class="text">{{ trans('general.reset') }}</span></button>
        </div>
    </article>
    </form>
{% endblock %}