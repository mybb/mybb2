{% extends "layouts.base" %}
{% block title %}
    {{ trans('passwords.resetpw') }}
{% endblock %}
{% block contents %}
	{# Kept here as 'status' is used by Laravels handler and not used by us #}
    {% if session_has('status') %}
        <div class="alert alert--success">
            {{ session_get('status') }}
        </div>
    {% endif %}

    {{ form_open({'url': ['password/email'], 'method': 'post'}) }}
        <div class="page-content page-content--centered">

            <header>
                <h1>{{ trans('passwords.resetpw') }}</h1>
            </header>

            <section class="form">
                <div class="form__section">
                    <div class="form__row">
                        <h3>{{ form_label('email', trans('member.email')) }}</h3>
                        {{ form_text('email') }}
                    </div>
                </div>
            </section>

            <div class="form__submit">
                {{ form_button(trans('general.reset'), {'type': 'submit', 'class': 'button icon fa-key'}) }}
            </div>
        </div>
    {{ form_close() }}
{% endblock %}