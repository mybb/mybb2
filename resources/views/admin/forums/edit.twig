{% extends "layouts.admin" %}
{% block title %} {{ trans('admin::forums.edit_forum') }} {% endblock %}
{% block contents %}
    <div class="admin-body__header">
        <h1 class="admin-body__header__title"> {{ trans('admin::forums.edit_forum') }} </h1>
    </div>
    <div class="admin-body__content">
        <form method="post" action="{{ url_route('admin.forums.edit', {'id': forum.id}) }}">
            {% include "admin.partials.forums.forum_form" with [forum, forums] %}
        </form>
    </div>
{% endblock %}
