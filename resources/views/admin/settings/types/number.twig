 <input
         type="number"
         name="setting[{{ setting.name }}]"
         value="{{ old('setting['~setting.name~']') ? old('setting['~setting.name~']') : setting.value }}"
         id="{{ setting.name }}"
 >
