{% extends "layouts.admin" %}
{% block title %}{{ trans('admin::users.add_title') }}{% endblock %}
{% block contents %}
    <div class="admin-body__header">
        <h1 class="admin-body__header__title">{{ trans('admin::users.add_title') }}</h1>
    </div>
    <div class="admin-body__content">
        {{ form_open({'route': ['admin.users.add'], 'method': 'post'}) }}
        {% include "admin.partials.users.user_form" %}
        {{ form_close() }}
    </div>
{% endblock %}
