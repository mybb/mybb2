{{ form_open({'route': ['moderate'], 'method': 'post', 'style': 'display: inline-block;'}) }}
    {{ form_hidden('moderation_name', moderation.key) }}
    {{ form_hidden('moderation_content', content_name) }}
    {{ form_hidden('moderation_ids[]', content_id) }}
    {{ form_button(moderation.name|capitalize, {'type': 'submit', 'class': 'button icon '~moderation.icon}) }}
{{ form_close() }}
