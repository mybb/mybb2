		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
		<meta name="description" content="{{ setting('general.board_desc') }}">
		<meta name="viewport" content="width=device-width,initial-scale=1,user-scalable=no">
		<link rel="stylesheet" href="http://fonts.googleapis.com/css?family=Open+Sans:400,600,700">
		{% if trans('general.direction') == 'rtl' %}
			<link rel="stylesheet" href="{{ asset('assets/css/rtl.css') }}">
		{% else %}
			<link rel="stylesheet" href="{{ asset('assets/css/main.css') }}">
		{% endif %}

		{# ReCaptcha needs to be loaded globally as we also support it on modals #}
		{# As the Captcha is loaded via JS we need the library first #}
		{% if setting('captcha.method') == 'recaptcha' %}
			<script type="text/javascript" src="//www.google.com/recaptcha/api/js/recaptcha_ajax.js"></script>
		{% elseif setting('captcha.method') == 'nocaptcha' %}
			<script src='https://www.google.com/recaptcha/api.js?render=explicit&hl={{ setting('user.language', 'en') }}'></script>
		{% endif %}