<select name="{{ name }}">

    {% for value, name in options %}
        <option value="{{ value }}" {{ value == selected ? 'selected="selected"' : '' }}>{{ name }}</option>
    {% endfor %}

</select>
