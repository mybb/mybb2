machine:
  timezone:
    Europe/London
  php:
    version: 5.6.22
  node:
    version: 6.1.0

dependencies:
    pre:
        - sudo composer selfupdate

test:
    pre:
        - composer sniff
    override:
        - composer test-unit-coverage

general:
    artifacts:
        - "tests/report"
